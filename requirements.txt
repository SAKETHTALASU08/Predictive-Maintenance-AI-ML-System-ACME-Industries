# ══════════════════════════════════════════════════════════════════════════════
# ACME Industries — Predictive Maintenance Platform
# Production Dependencies
# ══════════════════════════════════════════════════════════════════════════════

# ── Web Framework ──
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
streamlit>=1.41.0
requests>=2.32.0

# ── ML / Data Science ──
numpy>=1.26.0
pandas>=2.2.0
scikit-learn>=1.5.0
lightgbm>=4.5.0
imbalanced-learn>=0.12.0
scipy>=1.14.0

# ── NLP / Transformers ──
transformers>=4.46.0
torch>=2.4.0

# ── Visualization ──
matplotlib>=3.9.0
seaborn>=0.13.0

# ── Data Validation ──
pydantic>=2.9.0

# ── MLflow (for training scripts only) ──
mlflow>=2.17.0

# ══════════════════════════════════════════════════════════════════════════════
# NOTE: pyspark and databricks-connect are NOT included here.
# They are only required for the data pipeline phase (notebooks/data_ingestion_cleaning.py,
# feature_engineering.py, eda.py, pre_processing.py) — not for the inference API.
# Install separately if needed: pip install pyspark databricks-connect
# ══════════════════════════════════════════════════════════════════════════════
